import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.ninja77030.cyberneticarm',
  appName: 'cybernetic-arm',
  webDir: 'dist-react'
};

export default config;
